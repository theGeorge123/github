-- Creator Store assets used by the guardian-temple presentation.
-- The visible models are cloned by DebateWorldService after runtime sanitization.
-- Keep the matching cleaned model templates beside this module in ServerStorage.TempleAssets.
return table.freeze({
    GuardianSentinel = table.freeze({
        AssetId = 66718458,
        Source = "Creator Store (free)",
        UsedBy = table.freeze({"RIVET", "MOSS"}),
    }),
    GuardianOverlord = table.freeze({
        AssetId = 1141239424,
        Source = "Creator Store (free)",
        UsedBy = table.freeze({"PIP"}),
    }),
    TempleBanner = table.freeze({
        AssetId = 527549093,
        Source = "Creator Store (free)",
        UsedBy = table.freeze({"Left banner", "Right banner"}),
    }),
    FireBowl = table.freeze({
        AssetId = 9836925241,
        Source = "Creator Store (free)",
        UsedBy = table.freeze({"Four guardian fire bowls"}),
    }),
})
